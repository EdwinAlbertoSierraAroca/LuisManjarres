'use client';
// Componente legacy no usado: el formulario real vive en Editor.tsx.
// Se deja como placeholder para no romper imports antiguos.
export default function ProjectForm() {
  return null;
}
